---
description: 
globs: 
alwaysApply: false
---
# routes 目录规则

- 该目录用于存放路由定义文件，每个路由文件负责将请求分发到对应的控制器方法。
- 命名规范为 `xxx.routes.ts`，如 `user.routes.ts`、`session.routes.ts`。
- 路由文件通常包含：
  - 路由路径与 HTTP 方法的定义
  - 路由与控制器方法的映射
  - 路由级中间件的注册
- 路由文件应保持简洁，避免包含业务逻辑。
- 路由应尽量按资源或模块划分，便于维护和扩展。
