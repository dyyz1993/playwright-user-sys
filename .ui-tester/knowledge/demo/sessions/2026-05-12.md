# Session: demo-test-1778586331
Date: 2026-05-12
Module: demo
Scenarios: 7 passed / 0 failed

## Tested
- Demo page initial load: PASSED - Page loads with "开始体验" button
- Click start experience: PASSED - Session created, viewer iframe loaded
- WS connection and frame streaming: PASSED - Frames arriving at ~26 FPS, status "已连接"
- Navigation to test page: PASSED - URL filled, Enter dispatched, navigation logged
- Mouse click interaction (640,400): PASSED - Click dispatched to nested iframe
- Mouse click interaction (300,200): PASSED - Click dispatched to nested iframe
- Keyboard input (Hello): PASSED - Key events dispatched
- Standalone browser viewer: PASSED - Connected, frame #1200→#2500, binary frames streaming

## Findings
- Demo page uses double iframe nesting: demo → browser-viewer iframe → another browser-viewer iframe
- Remote browser display is via <img id="bv-screen"> with blob URL, NOT canvas
- Two WS connections established:
  1. /ws/{sessionId}/stream - Binary frames (~48KB and ~30KB alternating)
  2. /ws/{sessionId}/events - JSON messages (config, init)
- Frame rate: ~26 FPS (frame counter increments ~26/sec)
- Remote browser resolution: 1280x800
- Session idle timeout: 300 seconds
- No console errors observed during testing
- BV logs ([BV] prefix) not captured after injection - they may only appear during initial load

## WS Connection Details
- Stream WS URL: ws://192.168.0.29:3011/ws/{sessionId}/stream?token={token}
- Events WS URL: ws://192.168.0.29:3011/ws/{sessionId}/events?token={token}
- Stream frames: alternating binary 48909B and 29842B
- Events frames: JSON with configSync (fps:15, interactionMode, touchMode)

## Updated
- Created selectors.yml for demo module
- Created patterns.yml with all discovered interaction patterns
- Verified viewer module selectors still valid for browser-viewer page
