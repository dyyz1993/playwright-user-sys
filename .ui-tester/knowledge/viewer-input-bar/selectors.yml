# Module: viewer-input-bar
# Site: http://192.168.0.29:3011
# Version: 1
# Status: verified
# Last verified: 2026-05-16
# Updated in round: r1

viewer_page:
  url_pattern: "/viewer?sessionId={sessionId}&token={token}"
  events_websocket: "ws://{host}/ws/{sessionId}/events?token={token}"
  stream_websocket: "ws://{host}/ws/{sessionId}/stream?token={token}"
  
  # DOM elements
  video_display: "#video-display"
  touch_layer: "#touch-layer"
  fps_display: "#fps-display"
  bandwidth_display: "#bandwidth-display"
  btn_touchpad: "#btn-touchpad"
  btn_keyboard: "#btn-keyboard"
  btn_clip: "#btn-clip"
  keyboard_modal: "#keyboard-modal"
  keyboard_input: "#keyboard-input"
  keyboard_close: "#keyboard-close"
  keyboard_submit: "#keyboard-submit"

remote_test_page:
  url: "http://192.168.0.29:3011/test-interactive.html"
  first_text_input: "input[type='text']"
  textarea: "textarea"
  dynamic_input_react: "#dynamic-input-react"
  click_counter: "#clickCount"
