# 会话管理模块实现计划

## 模块概述

会话管理模块包含 6 个功能点,用于查看、管理和统计用户会话信息。

## 功能列表

| 功能 ID | 功能名称 | 优先级 | 状态 | 说明 |
|---------|----------|--------|------|------|
| P1-S11 | 会话统计数据 | P1 | 需实现 | 需要添加统计 API |
| P1-S12 | 会话详情页 | P1 | 需实现 | 需要添加详情页面 |
| P1-S13 | 按创建时间排序 | P1 | 需实现 | 需要添加排序参数 |
| P1-S14 | 按消耗积分排序 | P1 | 需实现 | 需要添加排序参数 |
| P1-S15 | 刷新会话列表 | P1 | 需增强 | 需要自动刷新功能 |
| P1-S16 | 实时状态显示 | P1 | 需实现 | 需要 WebSocket 或轮询 |

## 现有实现分析

### 已实现的功能

1. **会话列表页面**: `src/views/pages/sessions.ejs`
2. **会话 Model**: `src/models/session.model.ts`
3. **分页查询**: `SessionModel.paginate()`
4. **基础筛选**: 状态、用户、时间范围

### 已有的 API

1. **GET /admin/sessions** - 会话列表页面(服务端渲染)
2. **POST /api/admin/sessions/batch-release** - 批量结束会话
3. **POST /api/sessions/:id/release** - 结束单个会话

### 需要增强的功能

1. **会话统计**: 总会话数、活跃会话数、消耗积分统计
2. **会话详情**: 单个会话的详细信息页面
3. **排序功能**: 按创建时间、消耗积分排序
4. **自动刷新**: 定期刷新会话列表
5. **实时状态**: 显示会话实时状态

## API 设计文档

### 1. 获取会话列表(JSON API)

**端点**: `GET /api/admin/sessions`

**请求参数**:
```typescript
{
  page?: number;        // 页码,默认 1
  limit?: number;       // 每页数量,默认 20
  sort?: string;        // 排序字段: created_at|duration|credits_used|updated_at
  order?: string;       // 排序方向: asc|desc
  status?: string;      // 状态筛选: active|ended|error
  userId?: number;      // 用户ID筛选
  startDate?: string;   // 开始日期
  endDate?: string;     // 结束日期
  dateRange?: string;   // 时间范围: all|today|yesterday|week|month
}
```

**响应格式**:
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "uuid",
        "user_id": 1,
        "username": "testuser",
        "machine_id": "machine-uuid",
        "port": 3000,
        "status": "connected",
        "start_time": "2024-01-01T10:00:00.000Z",
        "end_time": null,
        "duration": 300,
        "credits_used": 5,
        "last_activity": "2024-01-01T10:05:00.000Z",
        "created_at": "2024-01-01T10:00:00.000Z"
      }
    ],
    "total": 100,
    "page": 1,
    "limit": 20,
    "totalPages": 5
  }
}
```

### 2. 获取会话统计

**端点**: `GET /api/admin/sessions/stats`

**请求参数**:
```typescript
{
  dateRange?: string;   // 时间范围
}
```

**响应格式**:
```json
{
  "success": true,
  "data": {
    "total": 1000,
    "active": 50,
    "ended": 900,
    "error": 50,
    "totalCreditsUsed": 5000,
    "totalDuration": 300000,
    "avgDuration": 300,
    "byUser": [
      {
        "user_id": 1,
        "username": "testuser",
        "sessionCount": 10,
        "creditsUsed": 50
      }
    ]
  }
}
```

### 3. 获取会话详情

**端点**: `GET /api/admin/sessions/:id`

**响应格式**:
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "user_id": 1,
    "username": "testuser",
    "machine_id": "machine-uuid",
    "machine_name": "machine-01",
    "port": 3000,
    "status": "connected",
    "options": {
      "headless": true,
      "browser": "chromium"
    },
    "start_time": "2024-01-01T10:00:00.000Z",
    "end_time": null,
    "duration": 300,
    "credits_used": 5,
    "last_activity": "2024-01-01T10:05:00.000Z",
    "error_message": null,
    "screenshot_url": null,
    "created_at": "2024-01-01T10:00:00.000Z",
    "updated_at": "2024-01-01T10:05:00.000Z"
  }
}
```

### 4. 刷新会话状态

**端点**: `POST /api/admin/sessions/refresh-status`

**请求体**:
```typescript
{
  sessionIds?: string[];  // 可选,指定要刷新的会话ID列表
}
```

**响应格式**:
```json
{
  "success": true,
  "data": {
    "updated": 50,
    "sessions": [
      {
        "id": "uuid",
        "status": "connected"
      }
    ]
  }
}
```

## 实现步骤

### 步骤 1: 扩展 SessionModel

**文件**: `src/models/session.model.ts`

**需要添加的方法**:

```typescript
// 获取会话统计
static async getStats(filters?: {
  startDate?: Date;
  endDate?: Date;
}): Promise<{
  total: number;
  active: number;
  ended: number;
  error: number;
  totalCreditsUsed: number;
  totalDuration: number;
  avgDuration: number;
  byUser: Array<{
    user_id: number;
    username: string;
    sessionCount: number;
    creditsUsed: number;
  }>;
}>

// 获取会话详情(包含用户和机器信息)
static async getDetailById(id: string): Promise<(Session & {
  username: string;
  machine_name?: string;
}) | null>

// 排序分页查询
static async paginateSorted(
  page: number = 1,
  limit: number = 20,
  options?: {
    sort?: string;
    order?: 'asc' | 'desc';
    filters?: SessionFilterOptions;
  }
): Promise<PaginatedResponse<Session & { username: string }>>
```

**实现示例**:

```typescript
// 获取会话统计
static async getStats(filters?: {
  startDate?: Date;
  endDate?: Date;
}): Promise<{
  total: number;
  active: number;
  ended: number;
  error: number;
  totalCreditsUsed: number;
  totalDuration: number;
  avgDuration: number;
  byUser: Array<{
    user_id: number;
    username: string;
    sessionCount: number;
    creditsUsed: number;
  }>;
}> {
  try {
    let query = db('sessions').select('sessions.*', 'users.username')
      .leftJoin('users', 'sessions.user_id', 'users.id');

    if (filters?.startDate) {
      query = query.where('sessions.created_at', '>=', filters.startDate);
    }

    if (filters?.endDate) {
      const endDate = new Date(filters.endDate);
      endDate.setHours(23, 59, 59, 999);
      query = query.where('sessions.created_at', '<=', endDate);
    }

    const sessions = await query;

    // 统计数据
    const total = sessions.length;
    const active = sessions.filter((s: any) =>
      ['created', 'connected'].includes(s.status)
    ).length;
    const ended = sessions.filter((s: any) =>
      ['disconnected', 'expired', 'completed'].includes(s.status)
    ).length;
    const error = sessions.filter((s: any) => s.status === 'error').length;

    const totalCreditsUsed = sessions.reduce((sum, s: any) =>
      sum + (s.credits_used || 0), 0);
    const totalDuration = sessions.reduce((sum, s: any) =>
      sum + (s.duration || 0), 0);
    const avgDuration = total > 0 ? Math.round(totalDuration / total) : 0;

    // 按用户分组统计
    const byUserMap = new Map();
    sessions.forEach((s: any) => {
      const userId = s.user_id;
      if (!byUserMap.has(userId)) {
        byUserMap.set(userId, {
          user_id: userId,
          username: s.username,
          sessionCount: 0,
          creditsUsed: 0
        });
      }
      const user = byUserMap.get(userId);
      user.sessionCount++;
      user.creditsUsed += s.credits_used || 0;
    });

    return {
      total,
      active,
      ended,
      error,
      totalCreditsUsed,
      totalDuration,
      avgDuration,
      byUser: Array.from(byUserMap.values())
    };
  } catch (error) {
    console.error('获取会话统计失败:', error);
    return {
      total: 0,
      active: 0,
      ended: 0,
      error: 0,
      totalCreditsUsed: 0,
      totalDuration: 0,
      avgDuration: 0,
      byUser: []
    };
  }
}

// 获取会话详情
static async getDetailById(id: string): Promise<(Session & {
  username: string;
  machine_name?: string;
}) | null> {
  try {
    const session = await db('sessions')
      .select('sessions.*', 'users.username', 'machines.hostname as machine_name')
      .leftJoin('users', 'sessions.user_id', 'users.id')
      .leftJoin('machines', 'sessions.machine_id', 'machines.id')
      .where('sessions.id', id)
      .first();

    if (!session) return null;

    return {
      ...session,
      options: session.options ? (typeof session.options === 'string' ? JSON.parse(session.options) : session.options) : null
    };
  } catch (error) {
    console.error('获取会话详情失败:', error);
    return null;
  }
}
```

### 步骤 2: 添加 API 路由

**文件**: `src/routes/admin-api.routes.ts`

**需要添加的路由**:

```typescript
// 获取会话列表(JSON)
fastify.get('/api/admin/sessions', {
  preHandler: [authenticate],
  schema: {
    querystring: {
      type: 'object',
      properties: {
        page: { type: 'number' },
        limit: { type: 'number' },
        sort: { type: 'string', enum: ['created_at', 'duration', 'credits_used', 'updated_at'] },
        order: { type: 'string', enum: ['asc', 'desc'] },
        status: { type: 'string' },
        userId: { type: 'number' },
        dateRange: { type: 'string' }
      }
    }
  }
}, async (request, reply) => {
  // 实现逻辑
});

// 获取会话统计
fastify.get('/api/admin/sessions/stats', {
  preHandler: [authenticate]
}, async (request, reply) => {
  // 实现逻辑
});

// 获取会话详情
fastify.get('/api/admin/sessions/:id', {
  preHandler: [authenticate]
}, async (request, reply) => {
  // 实现逻辑
});

// 刷新会话状态
fastify.post('/api/admin/sessions/refresh-status', {
  preHandler: [authenticate]
}, async (request, reply) => {
  // 实现逻辑
});
```

### 步骤 3: 添加会话详情页面

**文件**: `src/views/pages/session-detail.ejs`

**需要实现的内容**:

```html
<div class="bg-white rounded-lg shadow">
  <div class="px-6 py-4 border-b border-gray-200">
    <h3 class="text-lg font-medium text-gray-700">会话详情</h3>
  </div>

  <div class="p-6">
    <!-- 基本信息 -->
    <div class="grid grid-cols-2 gap-6 mb-6">
      <div>
        <p class="text-sm text-gray-500">会话 ID</p>
        <p class="text-sm font-medium"><%= session.id %></p>
      </div>
      <div>
        <p class="text-sm text-gray-500">用户</p>
        <p class="text-sm font-medium"><%= session.username %></p>
      </div>
      <div>
        <p class="text-sm text-gray-500">状态</p>
        <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full">
          <%= session.status %>
        </span>
      </div>
      <div>
        <p class="text-sm text-gray-500">开始时间</p>
        <p class="text-sm font-medium"><%= new Date(session.start_time).toLocaleString() %></p>
      </div>
      <div>
        <p class="text-sm text-gray-500">持续时间</p>
        <p class="text-sm font-medium"><%= session.duration %> 秒</p>
      </div>
      <div>
        <p class="text-sm text-gray-500">消耗积分</p>
        <p class="text-sm font-medium"><%= session.credits_used %> 点</p>
      </div>
    </div>

    <!-- 会话选项 -->
    <% if (session.options) { %>
    <div class="mb-6">
      <h4 class="text-sm font-medium text-gray-700 mb-2">会话选项</h4>
      <pre class="bg-gray-100 p-4 rounded"><%= JSON.stringify(session.options, null, 2) %></pre>
    </div>
    <% } %>

    <!-- 操作按钮 -->
    <div class="flex space-x-3">
      <% if (['created', 'connected'].includes(session.status)) { %>
      <button id="end-session-btn" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md">
        结束会话
      </button>
      <% } %>
      <a href="/admin/sessions" class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-4 py-2 rounded-md">
        返回列表
      </a>
    </div>
  </div>
</div>
```

### 步骤 4: 更新会话列表页面

**文件**: `src/views/pages/sessions.ejs`

**需要添加的功能**:

1. **统计卡片**:
```html
<!-- 统计卡片 -->
<div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-6">
  <div class="bg-white rounded-lg shadow p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm text-gray-500">总会话数</p>
        <p class="text-2xl font-bold" id="total-sessions">-</p>
      </div>
      <div class="p-3 bg-blue-100 rounded-full">
        <i class="fas fa-list text-blue-500"></i>
      </div>
    </div>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm text-gray-500">活跃会话</p>
        <p class="text-2xl font-bold text-green-600" id="active-sessions">-</p>
      </div>
      <div class="p-3 bg-green-100 rounded-full">
        <i class="fas fa-play-circle text-green-500"></i>
      </div>
    </div>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm text-gray-500">总消耗积分</p>
        <p class="text-2xl font-bold text-yellow-600" id="total-credits">-</p>
      </div>
      <div class="p-3 bg-yellow-100 rounded-full">
        <i class="fas fa-coins text-yellow-500"></i>
      </div>
    </div>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm text-gray-500">平均时长</p>
        <p class="text-2xl font-bold text-purple-600" id="avg-duration">-</p>
      </div>
      <div class="p-3 bg-purple-100 rounded-full">
        <i class="fas fa-clock text-purple-500"></i>
      </div>
    </div>
  </div>
</div>
```

2. **排序下拉框**:
```html
<div class="ml-4">
  <select id="sort-select" class="block w-full py-2 px-3 border border-gray-300 bg-white rounded-md">
    <option value="created_at:desc">创建时间(新→旧)</option>
    <option value="created_at:asc">创建时间(旧→新)</option>
    <option value="duration:desc">持续时间(长→短)</option>
    <option value="duration:asc">持续时间(短→长)</option>
    <option value="credits_used:desc">消耗积分(多→少)</option>
    <option value="credits_used:asc">消耗积分(少→多)</option>
  </select>
</div>
```

3. **自动刷新功能**:
```javascript
// 自动刷新开关
const autoRefreshCheckbox = document.getElementById('auto-refresh');
let refreshInterval = null;

autoRefreshCheckbox?.addEventListener('change', () => {
  if (autoRefreshCheckbox.checked) {
    // 每10秒刷新一次
    refreshInterval = setInterval(() => {
      loadSessions();
      loadStats();
    }, 10000);
  } else {
    if (refreshInterval) {
      clearInterval(refreshInterval);
      refreshInterval = null;
    }
  }
});

// 加载统计数据
async function loadStats() {
  const response = await fetch('/api/admin/sessions/stats');
  const result = await response.json();

  if (result.success) {
    document.getElementById('total-sessions').textContent = result.data.total;
    document.getElementById('active-sessions').textContent = result.data.active;
    document.getElementById('total-credits').textContent = result.data.totalCreditsUsed;
    document.getElementById('avg-duration').textContent = result.data.avgDuration + '秒';
  }
}

// 排序功能
const sortSelect = document.getElementById('sort-select');
sortSelect?.addEventListener('change', () => {
  const [sort, order] = sortSelect.value.split(':');
  loadSessions({ sort, order });
});
```

### 步骤 5: 添加页面路由

**文件**: `src/routes/admin.routes.ts`

```typescript
// 会话详情页
fastify.get('/admin/sessions/:id', {
  preHandler: [authenticate, verifyAdmin]
}, async (request, reply) => {
  try {
    const { SessionModel } = await import('../models/session.model.js');
    const session = await SessionModel.getDetailById(
      (request.params as any).id
    );

    if (!session) {
      return reply.status(404).view('404.ejs');
    }

    return reply.view('pages/session-detail.ejs', {
      user: request.user,
      session,
      currentPage: 'sessions'
    });
  } catch (error) {
    request.log.error('获取会话详情失败:', error);
    return reply.status(500).send('获取会话详情失败');
  }
});
```

## 数据库设计

**表**: `sessions`

**现有字段已满足需求,无需修改**

**索引优化**:
```sql
CREATE INDEX idx_sessions_created_at ON sessions(created_at);
CREATE INDEX idx_sessions_duration ON sessions(duration);
CREATE INDEX idx_sessions_credits_used ON sessions(credits_used);
CREATE INDEX idx_sessions_status ON sessions(status);
CREATE INDEX idx_sessions_user_id ON sessions(user_id);
```

## 测试方案

**文件**: `tests/api/sessions.test.ts`

```typescript
import { test, expect } from '@playwright/test';

const BASE_URL = process.env.BASE_URL || 'http://localhost:3000';
const TOKEN = 'your-admin-token';

test.describe('会话管理 API', () => {
  test('GET /api/admin/sessions - 获取会话列表', async ({ request }) => {
    const response = await request.get(`${BASE_URL}/api/admin/sessions`, {
      headers: { Authorization: `Bearer ${TOKEN}` }
    });
    expect(response.status()).toBe(200);
    const data = await response.json();
    expect(data.success).toBe(true);
  });

  test('GET /api/admin/sessions?sort=credits_used&order=desc - 按积分排序', async ({ request }) => {
    const response = await request.get(
      `${BASE_URL}/api/admin/sessions?sort=credits_used&order=desc`,
      { headers: { Authorization: `Bearer ${TOKEN}` } }
    );
    expect(response.status()).toBe(200);
  });

  test('GET /api/admin/sessions/stats - 获取统计', async ({ request }) => {
    const response = await request.get(`${BASE_URL}/api/admin/sessions/stats`, {
      headers: { Authorization: `Bearer ${TOKEN}` }
    });
    expect(response.status()).toBe(200);
    const data = await response.json();
    expect(data.data).toHaveProperty('total');
    expect(data.data).toHaveProperty('active');
  });

  test('GET /api/admin/sessions/:id - 获取详情', async ({ request }) => {
    // 先获取一个会话ID
    const listResponse = await request.get(`${BASE_URL}/api/admin/sessions`, {
      headers: { Authorization: `Bearer ${TOKEN}` }
    });
    const listData = await listResponse.json();
    const sessionId = listData.data.items[0]?.id;

    if (sessionId) {
      const response = await request.get(`${BASE_URL}/api/admin/sessions/${sessionId}`, {
        headers: { Authorization: `Bearer ${TOKEN}` }
      });
      expect(response.status()).toBe(200);
      const data = await response.json();
      expect(data.success).toBe(true);
    }
  });
});
```

## 验收标准

1. ✅ 会话列表支持排序
2. ✅ 统计数据正确显示
3. ✅ 会话详情页正常工作
4. ✅ 自动刷新功能正常
5. ✅ 所有 E2E 测试通过

## 风险评估

- **风险等级**: 中
- **工作量**: 中 (3-4天)
- **依赖**: 无

## 注意事项

1. 排序功能需要数据库索引支持
2. 自动刷新需要控制频率,避免过度请求
3. 统计数据需要考虑性能优化
4. 会话详情需要正确解析 options JSON
