# 系统日志模块实现计划

## 模块概述

系统日志模块包含 3 个功能点,用于查看和管理系统操作日志。

## 功能列表

| 功能 ID | 功能名称 | 优先级 | 状态 | 说明 |
|---------|----------|--------|------|------|
| P1-A06 | 查看系统操作日志 | P1 | 需增强 | 页面和基础 API 已存在 |
| P1-A08 | 日志时间筛选 | P1 | 需实现 | 需要后端支持 |
| P1-A09/A10 | 文件上传页面 | P1 | 需验证 | 页面已存在 |

## 现有实现分析

### 已实现的功能

1. **操作日志页面**: `src/views/pages/logs.ejs`
2. **操作日志 Model**: `src/models/operation-log.model.ts`
3. **页面路由**: `GET /admin/logs` (src/routes/admin.routes.ts)
4. **基础日志查询**: OperationLogModel.findAll() 和 paginate()

### 已有的 API

目前操作日志数据通过页面渲染直接获取,没有独立的 JSON API 端点。

### 需要增强的功能

1. **时间筛选后端支持**: 当前只有前端筛选,需要后端支持
2. **操作类型筛选**: 增强后端筛选功能
3. **分页优化**: 改进分页查询性能

## API 设计文档

### 1. 获取操作日志列表

**端点**: `GET /api/admin/operation-logs`

**请求参数**:
```typescript
{
  page?: number;        // 页码,默认 1
  limit?: number;       // 每页数量,默认 20
  action?: string;      // 操作类型筛选
  startDate?: string;   // 开始日期 (ISO 8601)
  endDate?: string;     // 结束日期 (ISO 8601)
  dateRange?: string;   // 时间范围: all|today|yesterday|week|month
}
```

**响应格式**:
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": 1,
        "admin_id": 1,
        "action": "创建用户",
        "details": {
          "username": "testuser",
          "role": "user"
        },
        "target_user_id": 2,
        "username": "admin",
        "role": "管理员",
        "created_at": "2024-01-01T10:00:00.000Z"
      }
    ],
    "total": 100,
    "page": 1,
    "limit": 20,
    "totalPages": 5
  }
}
```

### 2. 导出操作日志

**端点**: `GET /api/admin/operation-logs/export`

**请求参数**:
```typescript
{
  format?: string;      // 导出格式: csv|excel,默认 csv
  action?: string;      // 操作类型筛选
  startDate?: string;   // 开始日期
  endDate?: string;     // 结束日期
  dateRange?: string;   // 时间范围
}
```

**响应**: CSV 或 Excel 文件

### 3. 获取操作统计

**端点**: `GET /api/admin/operation-logs/stats`

**请求参数**:
```typescript
{
  dateRange?: string;   // 时间范围: all|today|week|month
}
```

**响应格式**:
```json
{
  "success": true,
  "data": {
    "total": 1000,
    "byAction": {
      "创建用户": 50,
      "更新用户": 100,
      "删除用户": 10,
      "添加算力": 200,
      "创建会话": 300,
      "结束会话": 250
    }
  }
}
```

## 实现步骤

### 步骤 1: 扩展 OperationLogModel

**文件**: `src/models/operation-log.model.ts`

**需要添加的方法**:

```typescript
// 分页查询日志(带筛选)
static async paginate(
  page: number = 1,
  limit: number = 20,
  filters?: {
    action?: string;
    startDate?: Date;
    endDate?: Date;
  }
): Promise<PaginatedResponse<OperationLog & { username?: string; role?: string }>>

// 获取操作统计
static async getStats(filters?: {
  startDate?: Date;
  endDate?: Date;
}): Promise<{ total: number; byAction: Record<string, number> }>

// 按日期范围筛选
static async findByDateRange(
  startDate: Date,
  endDate: Date,
  page?: number,
  limit?: number
): Promise<PaginatedResponse<OperationLog>>
```

**实现示例**:

```typescript
// 分页查询日志(带筛选)
static async paginate(
  page: number = 1,
  limit: number = 20,
  filters?: {
    action?: string;
    startDate?: Date;
    endDate?: Date;
  }
): Promise<PaginatedResponse<OperationLog & { username?: string; role?: string }>> {
  try {
    const offset = (page - 1) * limit;

    // 构建查询
    let query = db('operation_logs')
      .select('operation_logs.*', 'users.username', 'users.role')
      .leftJoin('users', 'operation_logs.admin_id', 'users.id');

    // 应用筛选条件
    if (filters?.action) {
      query = query.where('operation_logs.action', filters.action);
    }

    if (filters?.startDate) {
      query = query.where('operation_logs.created_at', '>=', filters.startDate);
    }

    if (filters?.endDate) {
      const endDate = new Date(filters.endDate);
      endDate.setHours(23, 59, 59, 999);
      query = query.where('operation_logs.created_at', '<=', endDate);
    }

    // 执行查询
    const [logs, totalResult] = await Promise.all([
      query.clone()
        .orderBy('operation_logs.created_at', 'desc')
        .limit(limit)
        .offset(offset),
      query.count('operation_logs.id as count').first()
    ]);

    return {
      items: logs.map(log => ({
        ...log,
        details: log.details ? (typeof log.details === 'string' ? JSON.parse(log.details) : log.details) : null
      })),
      total: totalResult ? Number(totalResult.count) : 0,
      page,
      limit,
      totalPages: Math.ceil((totalResult ? Number(totalResult.count) : 0) / limit)
    };
  } catch (error) {
    console.error('分页查询操作日志失败:', error);
    return {
      items: [],
      total: 0,
      page,
      limit,
      totalPages: 0
    };
  }
}

// 获取操作统计
static async getStats(filters?: {
  startDate?: Date;
  endDate?: Date;
}): Promise<{ total: number; byAction: Record<string, number> }> {
  try {
    let query = db('operation_logs');

    if (filters?.startDate) {
      query = query.where('created_at', '>=', filters.startDate);
    }

    if (filters?.endDate) {
      const endDate = new Date(filters.endDate);
      endDate.setHours(23, 59, 59, 999);
      query = query.where('created_at', '<=', endDate);
    }

    const logs = await query;

    const byAction: Record<string, number> = {};
    logs.forEach((log: any) => {
      const action = log.action;
      byAction[action] = (byAction[action] || 0) + 1;
    });

    return {
      total: logs.length,
      byAction
    };
  } catch (error) {
    console.error('获取操作统计失败:', error);
    return { total: 0, byAction: {} };
  }
}
```

### 步骤 2: 添加 API 路由

**文件**: `src/routes/admin-api.routes.ts`

**需要添加的路由**:

```typescript
// 获取操作日志列表
fastify.get('/api/admin/operation-logs', {
  preHandler: [authenticate],
  schema: {
    querystring: {
      type: 'object',
      properties: {
        page: { type: 'number', default: 1 },
        limit: { type: 'number', default: 20 },
        action: { type: 'string' },
        startDate: { type: 'string' },
        endDate: { type: 'string' },
        dateRange: { type: 'string', enum: ['all', 'today', 'yesterday', 'week', 'month'] }
      }
    },
    tags: ['admin']
  }
}, async (request, reply) => {
  try {
    const query = request.query as any;
    const page = parseInt(query.page) || 1;
    const limit = parseInt(query.limit) || 20;

    // 构建筛选条件
    const filters: any = {};

    if (query.action) {
      filters.action = query.action;
    }

    // 处理时间范围
    if (query.dateRange && query.dateRange !== 'all') {
      const now = new Date();
      const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());

      if (query.dateRange === 'today') {
        filters.startDate = today;
      } else if (query.dateRange === 'yesterday') {
        const yesterday = new Date(today);
        yesterday.setDate(yesterday.getDate() - 1);
        filters.startDate = yesterday;
        filters.endDate = yesterday;
      } else if (query.dateRange === 'week') {
        const startOfWeek = new Date(today);
        startOfWeek.setDate(today.getDate() - today.getDay());
        filters.startDate = startOfWeek;
      } else if (query.dateRange === 'month') {
        const startOfMonth = new Date(now.getFullYear(), now.getMonth(), 1);
        filters.startDate = startOfMonth;
      }
    }

    // 手动指定日期范围
    if (query.startDate) {
      filters.startDate = new Date(query.startDate);
    }
    if (query.endDate) {
      filters.endDate = new Date(query.endDate);
    }

    const { OperationLogModel } = await import('../models/operation-log.model.js');
    const result = await OperationLogModel.paginate(page, limit, filters);

    return reply.send(result);
  } catch (error: any) {
    request.log.error('获取操作日志失败:', error);
    return reply.status(500).send({ success: false, error: '获取操作日志失败' });
  }
});

// 获取操作统计
fastify.get('/api/admin/operation-logs/stats', {
  preHandler: [authenticate],
  schema: {
    querystring: {
      type: 'object',
      properties: {
        dateRange: { type: 'string', enum: ['all', 'today', 'week', 'month'] }
      }
    },
    tags: ['admin']
  }
}, async (request, reply) => {
  try {
    const query = request.query as any;
    const filters: any = {};

    if (query.dateRange && query.dateRange !== 'all') {
      const now = new Date();
      const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());

      if (query.dateRange === 'today') {
        filters.startDate = today;
      } else if (query.dateRange === 'week') {
        const startOfWeek = new Date(today);
        startOfWeek.setDate(today.getDate() - today.getDay());
        filters.startDate = startOfWeek;
      } else if (query.dateRange === 'month') {
        const startOfMonth = new Date(now.getFullYear(), now.getMonth(), 1);
        filters.startDate = startOfMonth;
      }
    }

    const { OperationLogModel } = await import('../models/operation-log.model.js');
    const stats = await OperationLogModel.getStats(filters);

    return reply.send({ success: true, data: stats });
  } catch (error: any) {
    request.log.error('获取操作统计失败:', error);
    return reply.status(500).send({ success: false, error: '获取操作统计失败' });
  }
});
```

### 步骤 3: 更新前端页面

**文件**: `src/views/pages/logs.ejs`

**需要更新的内容**:

1. **使用 API 获取日志数据**:
```javascript
// 加载日志数据
async function loadLogs() {
  const params = new URLSearchParams();
  params.set('page', currentPage);
  params.set('limit', limit);

  if (filters.action) params.set('action', filters.action);
  if (filters.dateRange && filters.dateRange !== 'all') {
    params.set('dateRange', filters.dateRange);
  }

  const response = await fetch(`/api/admin/operation-logs?${params}`);
  const result = await response.json();

  if (result.success) {
    renderLogs(result.data.items);
    updatePagination(result.data);
  }
}
```

2. **添加操作统计显示**:
```javascript
// 加载统计数据
async function loadStats() {
  const params = new URLSearchParams();
  if (filters.dateRange) params.set('dateRange', filters.dateRange);

  const response = await fetch(`/api/admin/operation-logs/stats?${params}`);
  const result = await response.json();

  if (result.success) {
    renderStats(result.data);
  }
}
```

3. **改进筛选器**:
```javascript
// 时间范围筛选 - 通过后端
const dateRangeFilter = document.getElementById('date-range');
dateRangeFilter?.addEventListener('change', () => {
  const selectedRange = dateRangeFilter.value;
  filters.dateRange = selectedRange;
  loadLogs(); // 重新加载数据
  loadStats(); // 重新加载统计
});
```

### 步骤 4: 测试

**文件**: `tests/api/operation-logs.test.ts`

```typescript
import { test, expect } from '@playwright/test';

const BASE_URL = process.env.BASE_URL || 'http://localhost:3000';
const TOKEN = 'your-admin-token';

test.describe('操作日志 API', () => {
  test('GET /api/admin/operation-logs - 获取日志列表', async ({ request }) => {
    const response = await request.get(`${BASE_URL}/api/admin/operation-logs`, {
      headers: { Authorization: `Bearer ${TOKEN}` }
    });
    expect(response.status()).toBe(200);
    const data = await response.json();
    expect(data.success).toBe(true);
    expect(data.data).toHaveProperty('items');
  });

  test('GET /api/admin/operation-logs?dateRange=today - 今日日志', async ({ request }) => {
    const response = await request.get(`${BASE_URL}/api/admin/operation-logs?dateRange=today`, {
      headers: { Authorization: `Bearer ${TOKEN}` }
    });
    expect(response.status()).toBe(200);
    const data = await response.json();
    expect(data.success).toBe(true);
  });

  test('GET /api/admin/operation-logs/stats - 获取统计', async ({ request }) => {
    const response = await request.get(`${BASE_URL}/api/admin/operation-logs/stats`, {
      headers: { Authorization: `Bearer ${TOKEN}` }
    });
    expect(response.status()).toBe(200);
    const data = await response.json();
    expect(data.success).toBe(true);
    expect(data.data).toHaveProperty('total');
    expect(data.data).toHaveProperty('byAction');
  });
});
```

### 步骤 5: 验证 E2E 测试

确保 `tests/e2e/p1-important.spec.ts` 中的测试通过:

```typescript
test('P1-A06: 应该能查看系统操作日志', async ({ page }) => {
  await page.goto(`${BASE_URL}/admin/logs`);
  // ... 现有测试代码
});

test('P1-A08: 操作日志应该支持按时间筛选', async ({ page }) => {
  await page.goto(`${BASE_URL}/admin/logs`);
  // 选择时间范围
  await page.selectOption('#date-range', 'today');
  // 验证结果
  // ... 现有测试代码
});
```

## 数据库设计

**表**: `operation_logs`

**现有字段**:
```sql
- id: INTEGER PRIMARY KEY
- admin_id: INTEGER
- action: VARCHAR(255)
- details: TEXT (JSON)
- target_user_id: INTEGER
- created_at: DATETIME
- updated_at: DATETIME
```

**索引优化**:
```sql
CREATE INDEX idx_operation_logs_created_at ON operation_logs(created_at);
CREATE INDEX idx_operation_logs_action ON operation_logs(action);
CREATE INDEX idx_operation_logs_admin_id ON operation_logs(admin_id);
```

## 验收标准

1. ✅ 操作日志列表正确显示
2. ✅ 时间筛选功能正常工作
3. ✅ 操作类型筛选正常工作
4. ✅ 分页功能正常
5. ✅ 统计数据正确显示
6. ✅ 所有 E2E 测试通过

## 风险评估

- **风险等级**: 低
- **工作量**: 中 (2-3天)
- **依赖**: 无

## 注意事项

1. 时间筛选需要考虑时区问题
2. 大量日志数据时需要考虑性能优化
3. 日志详情需要正确解析 JSON
4. 分页查询需要使用索引优化
