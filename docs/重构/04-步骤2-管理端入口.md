# 步骤 2: 重构管理端入口

## 文档信息

- **文档**: 04-步骤2-管理端入口.md
- **依赖**: [03-步骤1-共享代码目录.md](./03-步骤1-共享代码目录.md)
- **优先级**: P1
- **预估时间**: 2小时
- **状态**: 待执行

---

## 1. 目标

将管理端的入口代码从 `src/app.ts` 移动到 `src/manager/app.ts`，创建独立的管理端目录结构。

### 1.1 需要创建的文件

| 文件 | 说明 |
|------|------|
| `src/manager/app.ts` | 管理端应用构建逻辑 |
| `src/manager/server.ts` | 管理端启动入口 |

### 1.2 需要更新的文件

| 文件 | 操作 |
|------|------|
| `src/server.ts` | 更新为指向新的管理端入口 (向后兼容) |

---

## 2. 详细步骤

### 步骤 2.1: 创建管理端目录

```bash
mkdir -p src/manager
```

### 步骤 2.2: 创建管理端 app.ts

**新建文件: `src/manager/app.ts`**

从 `src/app.ts` 移植代码，修改导出函数名：

```typescript
import Fastify, { FastifyInstance } from 'fastify';
import { env } from '../config/env.js';
import { db, initDatabase } from '../config/database.js';
import { runMigrations } from '../models/migrations.js';
import routes from '../routes/index.js';
import plugins from '../plugins/index.js';
import { logger } from '../utils/logger.js';
import { NativeWebSocketProxyService } from '../services/native-websocket-proxy.service.js';

/**
 * 重置所有机器状态
 */
async function resetAllMachineStatus(): Promise<void> {
  try {
    await db('machines').update({ status: 'offline' });
    logger.info('所有机器已重置为离线状态');
  } catch (error) {
    logger.error('重置机器状态失败:', error);
  }
}

let wsProxyService: NativeWebSocketProxyService;

/**
 * 构建管理端应用（但不启动）
 */
export async function buildManager(): Promise<FastifyInstance> {
  const app = Fastify({
    logger: process.env.NODE_ENV !== 'test' ? {
      level: 'info',
      transport: {
        target: 'pino-pretty',
        options: {
          translateTime: 'HH:MM:ss Z',
          ignore: 'pid,hostname',
        },
      },
    } : false,
  });

  if (process.env.NODE_ENV === 'test') {
    await initDatabase();
  }

  wsProxyService = new NativeWebSocketProxyService(app.server);
  logger.info('✅ 原生WebSocket代理服务已初始化');

  logger.info('开始注册插件...');
  await app.register(plugins);
  logger.info('插件注册完成');

  logger.info('开始注册路由...');
  await app.register(routes);
  logger.info('路由注册完成');

  app.addHook('onClose', async () => {
    if (wsProxyService) {
      logger.info('正在关闭WebSocket代理服务...');
      wsProxyService.close();
    }
  });

  return app;
}

/**
 * 启动管理端服务
 */
export async function startManager() {
  try {
    await runMigrations();
    await resetAllMachineStatus();
    logger.info('所有机器状态已重置为离线');

    const { memoryStore } = await import('../services/memory-store.service.js');
    await memoryStore.loadInitialData();
    logger.info('内存存储服务已初始化');

    const { startGrpcServer } = await import('../services/machine-grpc.service.js');
    const grpcPort = parseInt(env.GRPC_PORT+'' || '50051', 10);
    startGrpcServer(grpcPort);

    const { startMachineMonitor } = await import('../services/machine-monitor.service.js');
    const monitorInterval = parseInt(env.MACHINE_MONITOR_INTERVAL+'' || '30000', 10);
    const machineMonitorTimer = await startMachineMonitor(monitorInterval);

    const { startCreditsMonitor } = await import('../services/credits-monitor.service.js');
    const creditsMonitorTimer = startCreditsMonitor(5000);
    logger.info('✅ 点数监控服务已启动，检查间隔: 5秒');

    const fastify = await buildManager();

    fastify.addHook('onClose', async () => {
      const { stopMachineMonitor } = await import('../services/machine-monitor.service.js');
      stopMachineMonitor(machineMonitorTimer);

      const { stopCreditsMonitor } = await import('../services/credits-monitor.service.js');
      stopCreditsMonitor(creditsMonitorTimer);
    });

    await fastify.listen({ port: env.PORT, host: '0.0.0.0' });

    logger.info(`✅ HTTP 服务器已启动: http://${env.HOST}:${env.PORT}`);
    logger.info(`✅ gRPC 服务器已启动: ${env.HOST}:${grpcPort}`);
    logger.info(`📚 API 文档: http://${env.HOST}:${env.PORT}/docs`);
  } catch (error) {
    logger.error('❌ 启动服务器失败:', error);
    process.exit(1);
  }
}

export default buildManager;
```

### 步骤 2.3: 创建管理端 server.ts

**新建文件: `src/manager/server.ts`**

```typescript
import { startManager } from './app.js';

// 处理未捕获的异常
process.on('uncaughtException', (error) => {
  console.error('❌ 未捕获的异常:', error);
  process.exit(1);
});

// 处理未处理的 Promise 拒绝
process.on('unhandledRejection', (reason) => {
  console.error('❌ 未处理的 Promise 拒绝:', reason);
});

// 启动管理端服务
startManager();
```

### 步骤 2.4: 更新原 server.ts (向后兼容)

**修改文件: `src/server.ts`**

```typescript
import { startManager } from './manager/app.js';

// 处理未捕获的异常
process.on('uncaughtException', (error) => {
  console.error('❌ 未捕获的异常:', error);
  process.exit(1);
});

// 处理未处理的 Promise 拒绝
process.on('unhandledRejection', (reason) => {
  console.error('❌ 未处理的 Promise 拒绝:', reason);
});

// 启动管理端服务 (向后兼容，指向新的管理端入口)
startManager();
```

### 步骤 2.5: 更新测试辅助文件

**修改文件: `src/tests/helpers/app.ts`**

将 `build()` 函数更新为从新的管理端入口导入，或者保持独立（因为测试需要轻量级应用）。

```typescript
import Fastify, { FastifyInstance } from 'fastify';
import { env } from '../../config/env.js';
import authPlugin from '../../plugins/auth.plugin.js';
import userRoutes from '../../routes/user.routes.js';
import authRoutes from '../../routes/auth.routes.js';
import adminApiRoutes from '../../routes/admin-api.routes.js';
import sessionRoutes from '../../routes/session.routes.js';
import machineRoutes from '../../routes/machine.routes.js';

// 测试应用保持独立，不依赖 manager/app.ts
export async function build(): Promise<FastifyInstance> {
  const app = Fastify({
    logger: false,
  });

  await app.register(authPlugin);
  await app.register(authRoutes);
  await app.register(userRoutes, { prefix: '/api/users' });
  await app.register(adminApiRoutes);
  await app.register(sessionRoutes, { prefix: '/api/sessions' });
  await app.register(machineRoutes, { prefix: '/api/machines' });

  return app;
}
```

---

## 3. 验证测试

### 3.1 运行验证脚本

```bash
node tests/verify-step2-manager.js
```

**预期结果:** 所有测试通过

### 3.2 验证内容说明

验证脚本 `tests/verify-step2-manager.js` 包含以下验证：

#### 1. 文件结构验证
- `src/manager/` 目录存在
- `src/manager/app.ts` 文件存在
- `src/manager/server.ts` 文件存在
- `src/server.ts` 保持向后兼容

#### 2. 导出功能验证
- `buildManager` 函数正确导出
- `startManager` 函数正确导出
- 默认导出正确

#### 3. 代码功能验证
- `buildManager` 是 async 函数
- 包含 `resetAllMachineStatus` 函数
- 正确导入 logger 和 WebSocketProxyService
- server.ts 正确处理异常

#### 4. 启动验证
- 服务能正常启动
- 检查关键启动日志

---

## 4. 回归测试

### 4.1 TypeScript 编译

```bash
pnpm build
```

**预期结果:** 无新增编译错误

### 4.2 API 测试

```bash
# 启动管理端
pnpm dev:server

# 在另一个终端运行 API 测试
pnpm test:api
pnpm test:login
```

**预期结果:** API 测试通过

---

## 5. 回滚方法

如果验证失败，执行以下回滚命令:

```bash
# 删除新创建的管理端目录
rm -rf src/manager

# 恢复原有的 server.ts
git checkout src/server.ts

# 删除验证脚本
rm tests/verify-step2-manager.js
```

---

## 6. 检查清单

执行完本步骤后，确认以下项目:

- [ ] `src/manager/` 目录已创建
- [ ] `src/manager/app.ts` 已创建并导出 `buildManager` 和 `startManager`
- [ ] `src/manager/server.ts` 已创建
- [ ] `src/server.ts` 已更新为指向新的管理端入口
- [ ] TypeScript 编译无新增错误
- [ ] API 测试通过
- [ ] 管理端能正常启动
- [ ] 验证脚本 `tests/verify-step2-manager.js` 通过

---

## 7. 完成标准

- ✅ 管理端代码成功移动到 `src/manager/`
- ✅ `src/manager/app.ts` 导出 `buildManager` 和 `startManager`
- ✅ `src/manager/server.ts` 可以独立启动管理端
- ✅ 向后兼容：`src/server.ts` 仍然可用
- ✅ 现有测试全部通过
- ✅ 验证脚本通过

---

## 8. 执行记录

### 8.1 执行时间
- 开始时间: 2024-12-25 13:00
- 完成时间: 2024-12-25 16:00
- 实际耗时: 约3小时

### 8.2 测试结果
| 测试项 | 执行命令 | 结果 | 备注 |
|--------|----------|------|------|
| 验证脚本 | `node tests/verify-step2-manager.js` | ✅ 通过 | 文件结构和导入验证 |
| API 测试 | `node tests/test-api.cjs` | ✅ 通过 | Health check 200 OK |
| 登录测试 | `node tests/test-login.cjs` | ✅ 通过 | 4/4 测试通过 |
| 服务启动 | `pnpm dev:server` | ✅ 通过 | HTTP:3000, gRPC:50051 |

### 8.3 遇到的问题
| 问题 | 解决方案 |
|------|----------|
| verify-step2-manager.js 有 undefined `run()` 调用错误 | 移除多余的 run() 调用，node:test 自动运行测试 |
| API 测试文件不存在 | 创建 tests/test-api.cjs 和 tests/test-login.cjs |
| Jest 无法解析 @shared 路径 | 更新 jest.config.cjs 添加 moduleNameMapper |

### 8.4 回滚记录
- 是否回滚: 否

### 8.5 Git 提交
- Commit Hash: `1f2cf07` (重构) / `400164f` (测试修复)
- Commit Message: `Step 2: Refactor manager entry point` / `Fix API tests and verification for Step 2`

---

## 9. 下一步

完成本步骤后，进入 [步骤 3: 重构机器端入口](./05-步骤3-机器端入口.md)

---

*文档创建日期: 2024-12-25*
*最后更新日期: 2024-12-25*
