<div class="bg-white rounded-lg shadow">
  <div class="px-6 py-4 border-b border-gray-200 flex justify-between items-center">
    <h3 class="text-lg font-medium text-gray-700">积分历史记录</h3>
    <div class="flex space-x-2">
      <button id="refresh-btn" class="bg-gray-100 hover:bg-gray-200 text-gray-700 px-3 py-2 rounded-md text-sm font-medium">
        <i class="fas fa-sync-alt"></i>
      </button>
    </div>
  </div>

  <div class="p-6">
    <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-6 space-y-4 md:space-y-0">
      <div class="flex items-center">
        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
            <i class="fas fa-search text-gray-400"></i>
          </div>
          <input type="text" id="search-history" placeholder="搜索用户..."
            class="block w-full pl-10 pr-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary-500 focus:border-primary-500">
        </div>
        <div class="ml-4">
          <select id="action-filter" class="block w-full py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-primary-500 focus:border-primary-500">
            <option value="">所有类型</option>
            <option value="add">添加</option>
            <option value="deduct">扣除</option>
            <option value="use">使用</option>
          </select>
        </div>
      </div>

      <div class="flex items-center space-x-4">
        <div>
          <label for="date-range" class="block text-sm font-medium text-gray-700 mb-1">日期范围</label>
          <select id="date-range" class="block w-full py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-primary-500 focus:border-primary-500">
            <option value="all">所有时间</option>
            <option value="today">今天</option>
            <option value="yesterday">昨天</option>
            <option value="week">本周</option>
            <option value="month">本月</option>
          </select>
        </div>
      </div>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              时间
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              用户
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              类型
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              数量
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              余额
            </th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              描述
            </th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% if (history && history.length > 0) { %>
            <% history.forEach(record => { %>
              <tr>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%= new Date(record.created_at).toLocaleString() %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="flex items-center">
                    <div class="flex-shrink-0 h-8 w-8 rounded-full bg-primary-500 flex items-center justify-center text-white font-medium">
                      <%= (record.username || 'U').charAt(0).toUpperCase() %>
                    </div>
                    <div class="ml-3">
                      <div class="text-sm font-medium text-gray-900"><%= record.username || '未知用户' %></div>
                      <div class="text-xs text-gray-500">ID: <%= record.user_id %></div>
                    </div>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <% if (record.action === 'add') { %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                      添加
                    </span>
                  <% } else if (record.action === 'deduct') { %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                      扣除
                    </span>
                  <% } else if (record.action === 'use') { %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">
                      使用
                    </span>
                  <% } else { %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">
                      <%= record.action %>
                    </span>
                  <% } %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm">
                  <span class="<%= record.action === 'add' ? 'text-green-600' : 'text-red-600' %>">
                    <%= record.action === 'add' ? '+' : '-' %><%= Math.abs(record.amount) %>
                  </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%= record.balance_after %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%= record.description || '-' %>
                </td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr>
              <td colspan="6" class="px-6 py-4 text-center text-gray-500">
                暂无积分历史记录
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <div class="flex items-center justify-between mt-6">
      <div class="text-sm text-gray-700">
        显示 <span class="font-medium"><%= (page - 1) * limit + 1 %></span> 到 <span class="font-medium"><%= Math.min(page * limit, totalRecords) %></span> 条，共 <span class="font-medium"><%= totalRecords %></span> 条
      </div>
      <div class="flex space-x-2">
        <% if (page > 1) { %>
          <a href="/admin/credits/history?page=<%= page - 1 %>&limit=<%= limit %>" class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">
            上一页
          </a>
        <% } else { %>
          <button disabled class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-400 bg-gray-100 cursor-not-allowed">
            上一页
          </button>
        <% } %>

        <% if (page * limit < totalRecords) { %>
          <a href="/admin/credits/history?page=<%= page + 1 %>&limit=<%= limit %>" class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">
            下一页
          </a>
        <% } else { %>
          <button disabled class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-400 bg-gray-100 cursor-not-allowed">
            下一页
          </button>
        <% } %>
      </div>
    </div>
  </div>
</div>

<script>
  const refreshBtn = document.getElementById('refresh-btn');
  refreshBtn?.addEventListener('click', () => {
    window.location.reload();
  });

  const searchInput = document.getElementById('search-history');
  searchInput?.addEventListener('input', () => {
    const searchTerm = searchInput.value.toLowerCase();
    const rows = document.querySelectorAll('tbody tr');

    rows.forEach(row => {
      const usernameCell = row.querySelector('td:nth-child(2) .text-sm.font-medium');
      if (usernameCell) {
        const username = usernameCell.textContent.toLowerCase();
        if (username.includes(searchTerm)) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      }
    });
  });

  const actionFilter = document.getElementById('action-filter');
  actionFilter?.addEventListener('change', () => {
    const selectedAction = actionFilter.value;
    const rows = document.querySelectorAll('tbody tr');

    rows.forEach(row => {
      const actionCell = row.querySelector('td:nth-child(3) span');
      if (actionCell) {
        let action = '';
        if (actionCell.textContent.trim() === '添加') action = 'add';
        else if (actionCell.textContent.trim() === '扣除') action = 'deduct';
        else if (actionCell.textContent.trim() === '使用') action = 'use';

        if (selectedAction === '' || action === selectedAction) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      }
    });
  });

  const dateRangeFilter = document.getElementById('date-range');
  dateRangeFilter?.addEventListener('change', async () => {
    const selectedRange = dateRangeFilter.value;
    const url = new URL(window.location.href);
    url.searchParams.set('dateRange', selectedRange);
    window.location.href = url.toString();
  });
</script>
